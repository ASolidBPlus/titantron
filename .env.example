# Titantron Configuration

# Log level (debug, info, warning, error)
TITANTRON_LOG_LEVEL=info

# Admin password (optional — leave empty for no password)
# TITANTRON_ADMIN_PASSWORD=

# Jellyfin connection (configured via setup wizard, but can be set here)
# TITANTRON_JELLYFIN_URL=http://your-jellyfin-server:8096

# Public-facing Jellyfin URL (if different from JELLYFIN_URL, e.g. behind reverse proxy)
# Used for stream/trickplay URLs sent to the browser
# TITANTRON_JELLYFIN_PUBLIC_URL=https://jellyfin.example.com
