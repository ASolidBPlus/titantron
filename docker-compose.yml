services:
  titantron:
    build: .
    ports:
      - "8765:8765"
    volumes:
      - titantron-data:/data
    env_file:
      - .env
    environment:
      - TITANTRON_DB_PATH=/data/titantron.db
      - TITANTRON_FRONTEND_DIR=/app/frontend-build
    restart: unless-stopped

volumes:
  titantron-data:
